[project]
name = "llm-polish"
version = "0.1.0"
description = "Context-aware LLM polish over a draft translation (local Ollama)."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "common-schemas",
    "httpx>=0.27.0",
]

[tool.uv.sources]
common-schemas = { path = "../../../../libs/common-schemas", editable = true }
